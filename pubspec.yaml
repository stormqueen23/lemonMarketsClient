name: lemon_market_client
description: Simple client to access lemonMarkets API
version: 0.0.1
author: Melanie Siekmöller
homepage: https://github.com/stormqueen23/lemonMarketClient

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.0.1
  http: ^0.13.3
  json_annotation: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.0.2
  json_serializable: ^4.0.3
flutter: